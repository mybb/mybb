<div class="alert {% if options.class is defined and options.class is not empty %}{{ options.class }}{% else %}alert--pending{% endif %}"{% if options.id is defined and options.id is not empty %} id="{{ options.id }}"{% endif %}>
    {% block message_before %}
        {{ include('partials/icon.twig', {icon: 'exclamation-triangle', class: 'alert__icon'}, with_context = false) }}
    {% endblock message_before %}
    <div class="alert__message">
        {{ options.message|raw }}
    </div>
    {% block message_after %}{% endblock message_after %}
</div>
