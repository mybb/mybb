{% if modpermissions.ismod %}
    <div class="thread__checkbox">
        <input type="checkbox" class="checkbox checkbox--select-row" name="inlinemod_{{ thread.tid }}" id="inlinemod_{{ thread.tid }}" value="1"{% if thread.modChecked %} checked{% endif %}  />
    </div>
{% endif %}
